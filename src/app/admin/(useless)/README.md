# (useless) — unused/dead code holding pen

Everything here was confirmed to have **zero live importers** (or, for `video`/`recordings`, no link anywhere in the admin nav) before being moved. Nothing was deleted — just parked here for you to review and decide: restore, finish, or delete.

- **video/**, **recordings/** — pages exist and still resolve at `/admin/video` and `/admin/recordings`, but nothing in the sidebar links to them anymore. `/api/save-recording` (still live, not moved) is what a device would POST to; these are just the viewer pages.
- **reward/** — a whole Reward feature (form, card, delete/crop dialogs, action functions) with no page route at all. `CreateRewardForm.tsx`/`EditRewardForm.tsx` were already fully commented out before this move.
- **legacy-student-table/** — an older "Student" admin table (columns/actions/filters/export) fully superseded by the current `/admin/member` pages. Zero importers. `action.ts` holds `getAllStudentUsers` (moved from `action/user.ts` — its only caller was this table's export-csv).
- **legacy-bin-issue-table/** — an empty, never-finished bin-issue table component plus its unused resolve-dialog. `action.ts` holds `updateBinStatus` (moved from `action/bin.ts` — its only caller was this dialog).
- **legacy-bin-dashboard/** — `binDashboard.tsx`, the old chart-heavy `/admin/bin` dashboard; already fully commented out before this move (superseded by the current bins list page + the new `/admin` dashboard charts). `action.ts` holds `getDisposalsByFaculty` (moved from `action/bin.ts` — its only caller was this dashboard's `chart.tsx`).
- **misc-orphans/** — assorted single-file orphans: two unused redirect helpers, an unused Card variant, an unused Faculty combobox, an unused email-verification form, three empty leftover schema files, and the pre-(Admin)-redesign `app-sidebar.tsx`/`mobile-app-sidebar.tsx` (superseded by `components/(Admin)/admin-dashboard.tsx`).
- **dead-api-routes/** — a stray empty `troute.ts` (typo of `route.ts`, never a real route) and two `commented.ts` files (password-reset/verification resend endpoints, fully commented out — Next.js only picks up `route.ts`, so these never ran).
- **dead-actions/** — server actions from the old `action/user.ts`, `action/bin.ts`, and `action/binMaterial.ts` with **zero live callers** (found while splitting those files up into page-colocated `action.ts` files): `getAllBins`/`getTopTenUsers` (from `user.ts`), `getAllBinsWithMaterial` (from `binMaterial.ts`), and a whole dead dashboard tree from `bin.ts` — `fetchAll` → `fetchDashboardData`/`fetchUMBinsData` → `getBinCountsByStatus`/`getDisposals`/`getFaultyBins`, plus `getUsedMaterialsForBin`, `getBinCountsByMaterial`, `getBinDisposalsByTime`, `getDisposalDates`, `evaluateHardwareStatus`. All superseded by `action/dashboard.ts` (`getDashboardStats`) plus the two still-live chart functions `getBarChartData`/`getPieChartData`, which now live in `action/dashboard.ts` too.
